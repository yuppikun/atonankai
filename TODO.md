# TODO — 後で差し替えるべき箇所

## 運営者名（正式名称が決まったら差し替える）

現在はすべて仮の表記「あと何回。運営」を使っています。正式名称が決まったら、
以下のファイルの `<!-- TODO: 運営者名。... -->` コメントが付いている箇所をすべて差し替えてください。

- `index.html`（フッター `.foot__bottom`）
- `about.html`（本文「運営者情報」・フッター `.foot__bottom`）
- `privacy.html`（フッター `.foot__bottom`）
- `terms.html`（フッター `.foot__bottom`）
- `disclaimer.html`（フッター `.foot__bottom`）
- `contact.html`（フッター `.foot__bottom`）

検索する場合は `あと何回。運営` または `TODO: 運営者名` で全ファイルを検索してください。

## 問い合わせ先の設置

`contact.html` に、問い合わせフォームまたは連絡用メールアドレスを設置してください。
設置するまでは、架空の連絡先やダミーのメールアドレスを書かないこと。

設置後は、以下もあわせて更新してください。

- `privacy.html` の「アクセス解析について」欄（解析ツールを導入した場合）
- `contact.html` の文面全体（「準備中」という表現の削除）

## アクセス解析ツールを導入する場合

`privacy.html` の該当項目を更新し、導入するツール名と取得する情報の範囲を明記すること。

## 統計データの更新

厚生労働省の生命表・健康寿命などが新しい年度の値に更新された場合は、`assets/js/data.js` の
`LX_M` / `LX_F` / `LIFE_EXPECTANCY` / `HEALTHY_LIFE` などを更新し、`about.html` の
「生命表の再現精度」の数値（誤差 最大0.065年 / 0.24ポイント）も再フィッティング後の値に更新すること。
