# 修正指示書 v2 — 前回の実装をやり直す

前回の実装は、見た目・機能ともに要求水準を満たしていません。これから書く内容に沿って、
**遠慮なく前回の実装を上書き・作り直してください。** 部分的な調整ではなく、必要なら作り直すこと。

既存の計算ロジック（`assets/js/calc.js` / `assets/js/data.js` の数式と統計値）には触れないこと。
これはこれまでと変わりません。

作業の最後に `node test.js` と `verify_clock.js`、そして本書で新規作成する
`date_select_test.js` を実行し、すべて通ることを確認してください。

---

## STEP 0【最優先・重大バグ】生年月日が選べない

現状、年・月・日のプルダウンは表示されているが、**選択肢が入っていない、または機能していない。**
これによりサイトの入り口が使えない状態になっている。他の何より先にこれを直すこと。

### 直したら、必ず自動テストで実証すること

`date_select_test.js` を新規作成し、jsdom で以下を検証すること（`test.js` と同じ手法でよい）。

1. 年・月・日それぞれの `<select>` 要素が存在する
2. **年の `<option>` が実際に90個以上入っている**（現在年から100年分、降順）
3. **月の `<option>` が12個入っている**
4. **日の `<option>` が1つ以上入っている**（月に応じて28〜31個）
5. 年に `1990`、月に `5`、日に `20` を選択し、それぞれ `change` イベントを発火させたとき、
   年齢表示（`#ageHint` 等）が空文字列でなくなる
6. 2月を選んだときに、日の選択肢が29個以下になる（うるう年考慮）
7. これらすべてが失敗した場合、`process.exit(1)` してどこが空だったかを出力する

**このテストが通るまで、次のSTEPに進まないこと。** 前回、口約束の「実装した」報告だけで
実際には動いていなかった。今回は自動テストの合格をもって初めて「直った」とみなすこと。

---

## STEP 1 デザインの方向を転換する（重要な軌道修正）

前回「黒板にチョーク」という方向で実装させたが、これは利用者の意図と違っていた。
**今回は白基調に転換する。** 「大学の黒板」ではなく、**「精密な分析結果が印字された白い論文・レポート」**
のイメージ。手書きのチョークではなく、**きちんと組版された数式が、薄く上品に配置されている状態**を目指す。

### 1-1. 色（`assets/css/style.css` の `:root` を全面差し替え）

```css
--paper:       #FAFAF7;   /* 背景の白。真っ白にせず、わずかに温かみを */
--paper-deep:  #F2F0E9;   /* カードなど、面を1段沈める時 */
--ink:         #14181C;   /* 本文・見出しの黒。純黒にしない */
--ink-soft:    #5A6169;   /* 補助テキスト */
--hairline:    #D8D4C8;   /* 罫線・枠線 */
--formula:     #B9B2A0;   /* 背景の数式の色。ごく薄く */
--accent:      #1F3A5F;   /* 深い紺。主要な数字・重要な線に使う唯一の強い色 */
--accent-warm: #9C4B32;   /* 抑えた朱色。強調・警告のみに、控えめに使う */
```

蛍光色・チョーク調の暖色（オレンジ／ピンク／黄）は全廃する。**色数を絞ることが上品さの条件。**
主役の色は`--accent`（紺）ひとつで十分。

### 1-2. 背景の数式（このサイトの顔）

前回はチョークの手書き風フォント（Caveat）を使ったが、これが「安っぽい」原因の一つ。
**手書き風をやめ、印刷された論文のような、きちんとした組版のフォントに変える。**

- 数式のフォントは `EB Garamond` のイタリック、または `Newsreader` のイタリックを使う
  （Google Fontsから読み込み）。手書き風フォントは使わないこと
- 色は `--formula`、不透明度は `0.35〜0.5` 程度（前回より少し見えてよい。ただし本文と重ならない
  余白部分に配置し、可読性を絶対に損なわないこと）
- 傾きはつけない、または `-1deg〜1deg` のごく微小な範囲に留める（手書き感ではなく、
  わずかな有機的ズレ程度）
- 実際に使っている式を必ず含める（内容は前回指示と同じ。以下に再掲）

```
e(x) = ∫₀^∞ l(x+t) / l(x) dt
l(x) = exp( −∫₀ˣ μ(s) ds )
μ(x) = A + B·e^(Cx)
N = f · ∫₀^∞ S₁(t)·S₂(t) dt
P(both alive at t) = S₁(t) × S₂(t)
81.09    87.13    72.57    75.45
```

- 画面中央（題字とリード文が乗る帯）は数式の密度を大きく下げ、余白として活かすこと。
  数式は画面の**上端・下端・左右の余白**に寄せて配置し、中央の可読性を最優先すること
- スクロールに対するパララックスは、前回同様ごく弱く。`prefers-reduced-motion` では無効化

### 1-3. タイポグラフィ

- **本文**: `Zen Kaku Gothic New`（維持）
- **見出し**: `Shippori Mincho B1` は維持してよいが、**もっと大きく、字間を締めて、
  きちんと組んだ状態にする**（後述のタイトル処理を参照）
- **数値**: `Outfit`（維持。細身のウェイトを大きく使う）
- **数式**: `EB Garamond` イタリック（新規）
- **キャプション・出典**: `Roboto Mono`（維持）

### 1-4. ボタン・カードなどの見直し

- 白背景に合わせて、カードは `--paper-deep` の背景 + `--hairline` の細い枠線
- ボタンは `--accent`（紺）の1pxの枠線＋白地、ホバーで紺が薄く塗られる。影は使わないか、
  ごく弱い拡散影のみ
- 角丸は前回同様 `4px` 程度に抑える

---

## STEP 2 タイトルを「デザインされたロゴ」にする

現状「あと何回。」が普通の文字のまま置かれているだけで、ロゴとして作り込まれていない。
**以下のいずれか、またはこれらを組み合わせて、明確に「デザインされた」状態にすること。**

- 文字サイズを大幅に拡大し（画面幅の大部分を占める規模）、字間を `-0.02em` 程度に詰めて、
  1つの塊として構成する
- 句点「。」の部分だけ色を `--accent` にして、視線を止める焦点にする
- 題字の背後に、ごく薄い巨大な数字や記号（例:このサイトの核である `∫` や `e(x)` の一部）を
  文字と重ねて透かしのように配置し、「分析している感」を出す
- 題字の下に、定規で引いたような細い横罫を1本添える（手書きではなく直線・機械的な線）
- タイトル単体を一つのコンポーネントとして丁寧に組むこと。「本文の延長のフォントサイズを
  大きくしただけ」に見えないように

---

## STEP 3 トップページの文章を大幅に削る

### 3-1. 現状の問題

説明文が長すぎて読む前に離脱される。**トップページは「読ませる」のではなく「触らせる」ことを
目的にする。** 詳しい説明は about.html に譲る。

### 3-2. リード文（1〜2行のみに絞る）

```
あなたに残された週末は、あと何回だろう。
```

これ1行、または上記+もう1行まで。前回のような2文・3文構成の長いリードは禁止。

### 3-3. 説明文は原則削除

現状ある「平均寿命から引き算していない」「生存確率を積分して」といった説明の長文パラグラフは、
**トップページから削除する。** 代わりに、リード文のすぐ下に、次の一文だけを小さく添える。

```
平均寿命の引き算ではなく、厚生労働省の生命表から算出しています。→ 計算方法を見る
```

この「→ 計算方法を見る」は `about.html` へのリンクにすること。詳細を知りたい人だけが読みに行く構造。

### 3-4. 安心感の一文もコンパクトに

前回、独立したブロックで大きく表示させたが、それも簡潔にしてよい。

```
登録不要。入力内容は送信されません。
```

これを1行、小さなバッジのような見た目で、ボタンの近くに添える程度に留める。長い説明文にしないこと。

### 3-5. ボタン

「数えはじめる」ボタンは維持。表紙の要素は **題字・リード文1行・補足リンク1行・安心バッジ1行・
ボタン** のみで構成し、それ以外の文章要素をトップページに追加しないこと。

---

## STEP 4 フッターを大幅に簡潔にする

### 4-1. 現状の問題

出典の説明や計算方法の説明が、フッターにも長々と書かれていて、トップページ同様くどい。

### 4-2. 変更内容

フッターは以下の要素だけに絞る。**長い説明文パラグラフは置かない。**

- サイト名 + 一言（例:「終わりを数えると、今が濃くなる。」）
- ナビゲーションリンク:このサイトについて（about.html） / プライバシーポリシー / 利用規約 /
  免責事項 / お問い合わせ
- 出典は、リンクの羅列のみでよい（説明文なしで機関名とリンクだけ並べる。詳しい説明は
  about.html に既にあるはずなので重複させない）
- 「本サイトの計算結果は統計上の期待値であり、個人の寿命を予測するものではありません」という
  一文だけは残す（短い注意書きとして）
- 著作権表記

**about.html・privacy.html・terms.html・disclaimer.html・contact.html は前回作成済みのはずなので、
中身を削除する必要はない。** ただし、これらのページも STEP 1 の白基調デザインに合わせて
配色を更新すること（黒板調のまま放置しないこと）。

---

## STEP 5 実装後の確認

1. `node test.js` — 全項目通過
2. `node verify_clock.js` — 全項目通過（前回作成済みのはず。なければ作成すること）
3. `node date_select_test.js` — 全項目通過（本書STEP 0で新規作成したもの）
4. 全ページ（index / about / privacy / terms / disclaimer / contact）が白基調デザインに
   統一されていることをコードレベルで確認する（`--slate` 等、旧・黒板配色の変数が
   どこにも残っていないか `grep -rn "slate" assets/css/style.css` で確認し、残っていれば削除する）
5. `grep -rn "Dela Gothic One\|Caveat" assets/` を実行し、旧フォント指定が残っていないか確認する

すべて確認できたら、commitしてpushする。

```bash
git add -A
git commit -m "デザイン方向転換：白基調・分析的レイアウトへ、生年月日バグ修正、文章量を削減"
git push
```

pushしてから90秒待ち、以下を確認する。

```bash
sleep 90
curl -s -o /dev/null -w "%{http_code}\n" https://atonankai.pages.dev/
```

`200` が返ることを確認する。

---

## STEP 6 完了報告

すべて完了したら、リポジトリ直下に `FIX_DONE.md` を新規作成し、以下を書いてcommit・pushすること。

```markdown
# 修正完了報告

## 直した内容
- 生年月日選択のバグ修正内容
- デザインを白基調に転換した内容
- タイトルをどう「デザインされたロゴ」にしたか
- トップページの文章をどう削ったか
- フッターをどう簡潔にしたか

## テスト結果
- node test.js: 通過/不通過
- node verify_clock.js: 通過/不通過
- node date_select_test.js: 通過/不通過

## デプロイ確認
https://atonankai.pages.dev/ の応答コード
```

`FIX_DONE.md` の commit・push が終わったら、最後に `FIX_DONE_MARKER.txt` というファイルを作成し、
中身に `done` とだけ書いて保存すること（これはcommit不要）。

自力で解決できない問題にあたった場合のみ、`BLOCKED.md` に状況を書いて終了すること。
それ以外は、質問で立ち止まらず、判断して最後まで進めること。
