# 修正完了報告 v6

対象: トップページ改修（題字と透かしの重なり解消／背景数式の高密度化／文言差し替え／
ボタン刷新）、結果表示前の演出、フッター「出典」の折りたたみ化。

## 1. 題字と背景の透かしの重なり

**採用した案: 案A（透かしを完全に削除）。**

`index.html` の `<span class="logo__wm" aria-hidden="true">e(x)</span>` と、対応する
CSS（`.logo__wm`）を削除した。理由：透かしの位置はフォントサイズ・行数・ビューポート幅に
応じて動的に変わる `clamp()` ベースの題字と重なりやすく、位置調整（案B）や不透明度調整
（案C）では「あらゆる画面幅で確実に重ならない」という絶対条件を保証しづらいと判断した。
背景数式自体を修正2で大幅に強化したため、透かしがなくても表紙の「数式に囲まれた紙面」
という意匠は保たれている。

## 2. 背景の数式（高密度・衝突判定つき）

`assets/js/app.js` の `spawnEqBg`（旧 `buildEqBg`。関数群は `layoutEqBg` /
`pickEqItem` / `rectsCollide` / `eqForbiddenRect` など）を全面書き直し。

- **配置数**: デスクトップ（幅720px以上）で **52個**、モバイル（幅720px未満）で
  **26個**（要求レンジ 40〜60 / 20〜30 の範囲内）。
- **衝突判定の実装方式**: 要求どおり「候補座標を生成→DOMに追加して
  `getBoundingClientRect()` で実測→既配置分と交差判定→ダメなら座標を変えて再試行
  （最大50回）→諦めたら配置しない」という手順を実装。候補座標は、画面をセル分割して
  シャッフルしたグリッドから優先的に取り出す方式（グリッドを使い切ったら完全ランダムに
  フォールバック）で、高密度でも高い確率で空きスロットが見つかるようにした。
- **矩形間の余白**: バウンディングボックス同士は最低 **8px** の余白を確保
  （`rectsCollide(rect, other, 8)`）。
- **本文との衝突回避**: 題字・リード文・安心の一文・ボタンを内包する `.cover__inner`
  の実測バウンディングボックスに **24pxマージン** を加えたものを「禁止矩形」として登録し、
  数式の矩形と交差する候補は破棄。パーセンテージによる概算ではなく実測値を使うため、
  文言や行数が変わっても確実に本文を避けられる。
- **密度の演出**: 数式を3階層（実際に使う式＝大きめ・出現率16%、統計/確率/微積分の
  実在表現＝中サイズ・出現率36%、実測値の短い数値＝小さめ・出現率48%）に分け、
  フォントサイズは0.62rem〜1.0remの範囲でランダム化。小さいものほど多く出現する設計。
  不透明度は0.18〜0.38の範囲でサイズに応じたベース値±ばらつきを与え、かつ禁止矩形から
  50px未満の位置に配置された数式は不透明度を0.22以下に強制的に下げ、本文近くに
  濃いものが来ないようにした。
- **傾き**: 一切つけていない（`transform`によるrotateは使用せず、常に水平）。
- **内容**: 実際に使っている7式（e(x)、l(x)、μ(x)、q(x)、N、P(both alive at t)、S(t)）を
  必須文字列としてそのまま含み（`test.js`が存在チェック済み）、統計学・確率論・
  微積分の実在する表現（Σ、lim、E[X]、Var(X)、Γ(n)、∂P/∂t、Cov(X,Y)など）と、
  実測値（81.09、87.13、72.57、75.45、l(65)=0.896、l(90)=0.258）を混ぜて密度を出した。
- **リサイズ対応**: `resize`イベントに200msデバウンスを入れ、レイアウトを再計算・
  再配置する。フォント読み込み完了（`document.fonts.ready`）後に初回配置することで、
  Webフォント差し替えによる実測サイズのズレも防いでいる。

## 3. トップページの文言

- リード文: **「終わりを感じるほど、いまが笑うように輝きだす。」** に差し替え。
  「終わりを意識すること」と「だからこそ今が輝くこと」の両方を1文に収め、暗さで
  終わらせず明るさに着地させた。見た目は `font-size: clamp(1.4rem, 5.6vw, 2.05rem)`・
  `font-weight:700` に強化し、題字に次ぐ第二の主役として組んだ。
- 「平均寿命の引き算ではなく…→計算方法を見る」の一文は削除（統計の説明は
  about.htmlに一任）。
- 安心を伝える文言: **「登録なし。入力はこの端末の中だけ。すぐに結果が出ます。」** に
  変更し、ボタン直下に配置。
- 結果として表紙に残る文章要素は「題字・キャッチフレーズ・安心の一文・ボタン」の
  4つのみ。

## 4. ボタン（「知る。」）

- ラベルを「数えはじめる」から **「知る。」** に変更（句点込み、題字「あと何回。」と
  句点で対句になる設計）。
- サイズ: `min-height:76px`（≥72px要件を満たす）、`min-width:208px`、
  `padding-inline:2.6rem`。
- タイポグラフィ: `font-family:var(--f-disp)`（明朝体、題字と連続性）、
  `font-size:1.18rem`、`letter-spacing:.15em`。
- インタラクション: `::before`疑似要素を `transform:scaleY(0)`（`transform-origin:bottom`）
  で仕込み、ホバーで `scaleY(1)` にすることで「下から色が満ちる」アニメーションを実装
  （220ms、`cubic-bezier(.22,.85,.3,1)`）。同時に `translateY(-2px)` でごく微細に
  浮き上がり、影を追加。`:active`で `translateY(1px) scale(.985)` に沈み込む。
  トランジションはすべて180〜260msの範囲（220ms中心）。
- reduced-motion対応: サイト全体の既存ルール
  `@media (prefers-reduced-motion:reduce){ *{transition-duration:.01ms!important} }`
  がこのボタンにもそのまま適用され、アニメーションが実質無効化される（個別の
  追加実装は不要だった）。

## 5. 結果表示前の演出（離散→収束→閃光→結実）

`assets/js/app.js` に `runReveal()` とその補助関数群
（`collectRevealFragments` / `spawnRevealFragments` / `spawnRevealTrail` /
`spawnRevealFlash` / `staggerResultChildren`）を実装。STEP3の「結果を見る」クリックで
`go('result')`を直接呼ぶのではなく、`runReveal()`を呼ぶように変更した。

### 実装方式
CSS Animation ではなく **Web Animations API（`Element.animate()`）** を使用。
すべての動きは `transform` と `opacity` のみで構成し、レイアウト再計算を伴う
プロパティ（`left`/`top`の連続変化など）は使っていない（`left`/`top`は初期配置の
一度きりの指定のみ）。

### フェーズ1「離散」
`collectRevealFragments()` が `S`（生年月日・性別・現在年齢・目標年齢・睡眠/食事/
スマホ/動画の回答・親/子/大切な人/ペットの年齢と頻度・STEP2で選んだ項目名）から、
**本人が実際に入力/選択した値だけ**を集める（ダミー文字列は一切使わず、未入力項目は
単に含めない＝水増ししない）。デスクトップ最大18個・モバイル最大12個をランダムに
選び、画面周辺に散らして表示。EB Garamond（`--f-formula`）とRoboto Mono
（`--f-mono`）を交互に使用。各要素は `delay`を`i*(240/n)`msずつずらして出現させ
（最大約260ms）、一斉には現れない。

### フェーズ2「収束」
各断片は、出現位置から中心への直線ではなく、進行方向に垂直な制御点
（`revealArcControl`）を挟んだ2次ベジエ風の折れ線経路（4キーフレーム）で中心へ移動。
終盤に加速するイージング（`cubic-bezier(.7,0,.84,0)`）を経路後半のキーフレームに
指定し、中心に近づくほど速く見えるようにした。移動しながら`scale`を1→0.1へ、
`opacity`を1→0へ変化。デスクトップでは軌跡をSVG `<path>`（`stroke-dasharray`/
`stroke-dashoffset`のドロー演出）で一瞬だけ残す（モバイルは負荷軽減のため省略）。
1要素あたりの「出現+収束」合計は1100ms（出現200ms・収束900ms）。

### フェーズ3「閃光」
`spawnRevealFlash()`が、中心から広がる円形バースト（`radial-gradient`、白→
`--accent-warm`→`--accent`、`scale(0)→scale(1)`・`opacity:1→0`、350ms）、
8方向に伸びる細い光条（`scaleY(0)→scaleY(1)`、320ms）、画面全体のごく短い
明滅（`--accent-warm`を`opacity:0→.16→0`、260ms）を組み合わせて実行。

### フェーズ4「結実」
`showResultNow()`が`go('result')`を呼んで結果画面のDOMを構築（同期処理・数十ms
程度）した直後、`go()`が付与する画面全体フェード（`.screen-enter`）を明示的に
打ち消し、代わりに`staggerResultChildren()`が`#screen-result .wrap`の直下要素
（hero・headline・cards・share・actionsなど）を上から順に`opacity:0→1`・
`translateY(10px→0)`で段階的に見せる（1要素あたり420ms、ディレイは
`i*70ms`・最大400ms）。並行してオーバーレイ自体も450msかけて`opacity:1→0`し
消える。

### 全体の所要時間
実測でおよそ1.5〜2.3秒（オーバーレイ出現から消滅まで）。設計上のフェーズ内訳は
400ms＋900ms＋350ms＋450ms＝約2100msを目安にした。

### スキップ手段
演出用オーバーレイ（`.reveal`）全体に`click`・`touchstart`リスナーを付け、
どこをタップ/クリックしても即座に`showResultNow()`（未実行なら`go('result')`と
段階表示を実行）→`teardown()`（タイマー全解除・進行中アニメーション全キャンセル・
オーバーレイ除去）を呼ぶ。

### フォールバック
- **3秒のハードタイムアウト**: `setTimeout(skip, 3000)`を演出開始時に必ず仕込み、
  何が起きても3秒後には結果画面へ強制的に進む。
- **例外耐性**: オーバーレイ構築・断片アニメーション・`showResultNow`呼び出しの
  それぞれを`try/catch`で個別に保護し、1箇所が失敗しても他の断片・全体の演出・
  最終的な結果表示は継続する設計にした。
- **Web Animations未対応環境への対応**: `span.animate`/`kid.animate`が関数として
  存在するかを`typeof`で確認し、存在しない場合はその要素の演出だけを諦めて
  通常表示にフォールバックする（テスト中にjsdom環境で実際にこの分岐を通ることを
  確認済み）。
- **prefers-reduced-motion**: `runReveal()`の先頭で`reducedMotion()`を判定し、
  trueなら演出を一切実行せず`go('result')`を直接呼んで即座に結果画面を表示する。

### テストへの影響
`test.js`と`verify_clock.js`はjsdom上で`#toResult`をクリックして結果画面のDOMを
同期的に検証しているが、演出が約1.5〜2秒の非同期処理になったことで、そのままでは
検証前に結果画面が構築されない状態になっていた。両ファイルの`matchMedia`モックを
「`prefers-reduced-motion`のクエリには`matches:true`を返す」よう修正し、
テスト実行時は演出をスキップして`go('result')`が同期的に呼ばれるようにした
（演出そのものの見た目はPlaywrightで別途確認）。calc.js・data.jsの数式・統計値は
一切変更していない。

## 6. フッターの「出典」を折りたたみ化（追加対応）

初回反映後、「出典が全ページで直接見える状態はよくない」「クリックすると全部
見れるようにワンクッションいれて」という指摘を受けて追加修正。

- `index.html` / `about.html` / `privacy.html` / `terms.html` / `disclaimer.html` /
  `contact.html` の全ページで、フッターの
  `<h2 class="foot__h">出典</h2><ul class="srcs" id="srcList"></ul>` を
  `<details class="foot__srcs"><summary>出典</summary><ul class="srcs" id="srcList">…</ul></details>`
  に変更。ネイティブの`<details>/<summary>`を使うことで、追加のJSなしに
  「クリックで開閉」を実装。矢印アイコン（`▸`/`▾`、`summary::after`）で開閉状態を示す。
- 開閉のJS的な検証は、`getComputedStyle`/`getClientRects()`ベースの判定が
  今回のPlaywright同梱Chromium（HeadlessChrome/151、`<details>`の内部実装が
  slotベース）では不正確な値を返すことを確認したため、実際のスクリーンショット
  （後述）で目視確認する方式に切り替えた。
- `about.html`本文中の「使用している統計と出典」セクション（`#aboutSrcList`）は、
  そのページ自体が出典を説明する主題であるため、常時表示のまま維持した
  （フッターの繰り返し表示のみを問題視したため）。

## 目視確認の結果

Playwrightで実際に操作し、画像を読み込んで確認した。

**幅390px・1280pxの表紙**: 数式どうしの重なりゼロ、数式と`.cover__inner`
（題字・リード文・安心の一文・ボタン）の重なりゼロ、透かし削除済み、
リード文・安心文・ボタンラベルが意図通り。ローカル・本番（`atonankai.pages.dev`）
の両方で確認。

**ボタンのホバー**: `::before`の下からの色満ちが正しく発火し、文字色が
`--paper`に反転することを確認。

**結果表示前の演出**: クリック直後の`.reveal`要素出現タイミングに同期して
複数時点（150/500/1000/1350/1500/1750/2000/2300ms相当）でスクリーンショットを
撮影し、離散（本人の実データが散らばって出現）→収束（弧を描く軌跡と実測サイズに
基づき画面端をはみ出さない配置）→閃光（中心のバースト）→結実（結果画面の
段階的フェードイン）の順に意図通り推移することを確認。幅390px・1280pxの両方、
ローカル・本番の両方で確認。

**演出のスキップ**: オーバーレイ表示中に画面をクリックすると即座に結果画面へ
到達し、オーバーレイが除去されることを確認。

**reduced-motion環境**: Playwrightの`reducedMotion:'reduce'`設定でアクセスすると、
`.reveal`オーバーレイが一切生成されず、即座に結果画面へ遷移することを確認。

**フッターの出典折りたたみ**: 閉じた状態では「出典 ▸」のみが表示され、一覧
（厚生労働省・総務省統計局などの出典6件）が画面に描画されないこと、クリック後は
「出典 ▾」に変わり一覧が展開されることを、実際のスクリーンショットで確認
（`<details>`の`open`属性のtrue/false切り替わりもJSで確認済み）。about.htmlで
ローカル・本番の両方を確認。

## アセットバージョン

`?v=20260815g`（`index.html` / `about.html` / `privacy.html` / `terms.html` /
`disclaimer.html` / `contact.html` の全HTMLファイルで統一して更新済み。今回の
一連の変更は `20260815f`→`20260815g` の2段階でpushしており、現在の本番は
`20260815g`）。

## 確認できなかった項目・留意点

- 演出の「約2秒」という所要時間は、フェーズごとの設計値の合計（400+900+350+450≈2100ms）
  であり、実ブラウザでの体感時間は環境（CPU・GPU・フレームレート）によって
  多少前後する。今回の検証環境（Playwright + Headless Chromium）では概ね
  1.5〜2.3秒の範囲で収まることを確認したが、実機の低スペック端末での体感速度までは
  検証していない。
- Web Animations API非対応環境（非常に古いブラウザ）でのフォールバック動作は
  jsdom（Node.js環境）でのみ確認しており、実際の旧ブラウザ実機では確認していない。
- 演出中の断片の総数・配置は毎回ランダムに変わる設計のため、今回のスクリーンショットは
  あくまで「代表的な1回の見え方」であり、全パターンを網羅したわけではない
  （ただし衝突検出・画面端クランプのロジック自体は複数の幅・複数回の実行で
  一貫して機能することを確認済み）。
