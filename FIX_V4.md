# 修正指示書 v4 — 時計と砂時計の描画を作り直す

## 現状の不具合（スクリーンショットで確認済み）

結果画面の人生時計が、以下の状態になっている。

1. **時計の円（外周のリング）が描画されていない。** 12 / 3 / 6 / 9 の数字だけが空中に浮いている
2. **短針・長針が1本も見えない。** 描かれていないか、砂時計の塗りの下に隠れている
3. **砂時計が砂時計に見えない。** 輪郭線がなく「砂の塗り」だけで描かれているため、
   上が満杯・下が空の状態では、ただの三角形が1つあるようにしか見えない

3番が設計上の根本問題である。**砂が入っていない側は完全に不可視になってしまう。**

---

## 直し方

現在の実装に固執せず、以下の仕様で**作り直すこと**。

### 1. 時計と砂時計を「入れ子」にするのをやめる

時計の中に砂時計を入れる構成は、要素が重なって視認性を著しく損なっている。
**時計と砂時計を分離し、横並び（またはモバイルでは上下）に配置すること。**

推奨レイアウト:

- デスクトップ（720px以上）:時計を左、砂時計を右に横並び。時計の方が大きい
- モバイル:時計を上、砂時計をその下に小さく配置。または砂時計を時刻表示の横に小さく添える

砂時計は時計より小さくてよい。ただし**形がはっきり読み取れるサイズ**を確保すること。

### 2. 時計の描画仕様

SVG で以下を、**この描画順で**描くこと。後に描いたものが上に来る。

1. **外周のリング** — `<circle>` で `fill="none"`、`stroke` に線の色、`stroke-width` は 1.5〜2 程度。
   **これが現在描かれていない。必ず描くこと。**
2. **目盛り** — 12方向に短い線。3時間ごと（12/3/6/9の位置）は少し長く・太くする
3. **数字** — 12 / 3 / 6 / 9 を円の内側に配置。`text-anchor="middle"` と
   `dominant-baseline="central"` で正確に中央揃えすること
4. **長針** — 細く長い線。長さは半径の 0.72 倍程度
5. **短針** — 太く短い線。長さは半径の 0.48 倍程度
6. **中心のドット** — 針の根元を隠す小さな円

**針は必ず最後に描くこと。** 他の要素より手前に来るようにする。

角度の計算（12時方向を0度、時計回り）:

```
短針の角度 = ((hour % 12) + minute / 60) / 12 * 360
長針の角度 = minute / 60 * 360
```

SVG の座標系では 0度が3時方向・反時計回りが正なので、変換に注意すること。
中心 (cx, cy) から角度 θ 度・長さ L の線の終点は:

```
x = cx + L * sin(θ * π / 180)
y = cy - L * cos(θ * π / 180)
```

**針が確実に見えるよう、`stroke` の色を明示すること。**
色は `--ink`（短針）と `--accent`（長針）など、背景と明確に異なる色を使う。
CSS変数がSVG属性で解決されない可能性がある場合は、JavaScript側で実際の色コードを
埋め込むこと。**「変数を書いたから大丈夫」で済ませず、実際に描画されるか確認すること。**

### 3. 砂時計の描画仕様

**最重要:砂の量にかかわらず、砂時計の形が常に見えること。**

以下の順で描く。

1. **砂時計の輪郭** — 上下2つの三角形が中央で接する形を、`<path>` で
   `fill="none"` かつ `stroke` ありで描く。**これを必ず最初に、常に描くこと。**
   砂がゼロでも満杯でも、この輪郭は常に表示される
2. **上下の枠（キャップ）** — 上端と下端に短い横線を引く。これがあると
   「砂時計」だと一目で分かる。省略しないこと
3. **上の砂** — 上の三角形の内側に、残量に応じた台形を塗る。
   `ratio`（経過割合）が 0 なら三角形いっぱい、1 なら消える
4. **下の砂** — 下の三角形の内側に、落ちた分に応じた台形を塗る。
   `ratio` が 0 なら消える、1 なら三角形いっぱい
5. **落ちている砂の筋** — 中央のくびれから下へ細い縦線を1本。任意（あってもなくてもよい）

砂の色は `--accent-warm` などの控えめな色。輪郭線は `--ink-soft` 程度。

**塗りの計算に注意:** 三角形の中に「下から高さhまで塗る」場合、
塗る領域は三角形ではなく台形になる。面積比ではなく高さ比で計算すると
見た目の量がずれるが、視覚的な分かりやすさを優先し、**高さ比で問題ない。**
ただし上の砂は「上から減っていく」ため、上端から減らす形になることに注意すること。

### 4. 砂時計に説明を添える

砂時計だけでは何を表しているか伝わらないため、すぐ下に小さく説明を置くこと。

```
人生の 2.4% が過ぎました
```

数値は経過割合（`ratio`）を使う。

---

## 検証【必須。これをやらずに完了と判断しないこと】

### 検証1 Playwright でスクリーンショットを撮り、画像を自分で読み込んで確認する

以下の3パターンで結果画面まで実際に操作して進み、時計と砂時計の部分を撮影すること。

| パターン | 生年月日の例 | 期待される見え方 |
|---|---|---|
| 若年 | 2005年生まれ | 時刻は早朝寄り、砂時計は上がほぼ満杯 |
| 中年 | 1985年生まれ | 時刻は昼前後、砂時計は上下が半々に近い |
| 高齢 | 1955年生まれ | 時刻は夕方以降、砂時計は下が多い |

撮影した画像に対して、以下を1つずつ確認すること。

- [ ] **時計の円（リング）が見えている**
- [ ] **短針と長針の2本が、はっきり見えている**
- [ ] 針の向きが、表示されている時刻と一致している
- [ ] 数字 12 / 3 / 6 / 9 が円周上の正しい位置にある
- [ ] **砂時計が「砂時計の形」として認識できる**（輪郭と上下の枠が見えている）
- [ ] 砂の量が、経過割合と見た目で一致している
- [ ] 時計と砂時計が重なって読めなくなっていない
- [ ] 幅390px（モバイル）でも崩れず、はみ出していない

**1つでも満たさなければ、直して撮り直し、満たすまで繰り返すこと。**

### 検証2 verify_clock.js

針の角度と表示時刻が数学的に一致することを、最低20パターンで検証すること。
ファイルが存在しなければ作成する。

---

## 完了手順

1. 実装する
2. `node test.js` / `verify_clock.js` / `date_select_test.js` を実行し、全て通す
3. 上記の検証1・検証2をすべて実施し、目視で問題がないことを確認する
4. **アセットのバージョン文字列 `?v=` を新しい値（例: `20260815d`）に更新する。**
   全HTMLファイルで更新すること。忘れると利用者に修正が届かない
5. commit して push する
6. 90秒待ってから、**本番URL** `https://atonankai.pages.dev/` に対して
   Playwright で検証1を再実施し、本番でも正しく表示されることを確認する
7. `FIX_V4_DONE.md` に以下を書いて commit・push する

```markdown
# 修正完了報告 v4

## レイアウト
- 時計と砂時計をどう配置したか（横並び / 上下 など）

## 時計
- リング・目盛り・数字・針の描画方法
- 針の色に何を指定したか（実際の色コード）
- 描画順をどうしたか

## 砂時計
- 輪郭を常時描画する実装にしたか
- 上下の枠（キャップ）を描いたか
- 砂の量の計算方法

## 目視確認の結果
3パターン（若年/中年/高齢）それぞれについて、
チェック項目8つの結果を記載すること。

## アセットバージョン
更新後の ?v= の値

## 確認できなかった項目
（あれば正直に書く。なければ「なし」）
```

質問で立ち止まらず、判断して最後まで進めること。
自力で解決できない場合のみ `BLOCKED.md` に状況を書いて終了すること。
