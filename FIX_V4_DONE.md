# 修正完了報告 v4

## レイアウト
時計と砂時計を「入れ子」構成から分離し、別々の `<svg>` として横並びに配置した。

- 実装: `assets/js/app.js` の `heroClockSVG()` を `clockFaceSVG()`（時計）と
  `hourglassSVG()`（砂時計）に分割し、`.hero__clockrow` の中に
  `.hero__clockface`（時計）と `.hero__hourglass`（砂時計＋説明文）を
  兄弟要素として並べる構成にした。
- デスクトップ（720px以上）: `flex-direction:row` で時計を左・砂時計を右に横並び。
  時計230px、砂時計110pxで時計の方が大きい。
- モバイル（720px未満）: `flex-direction:column` で時計を上、砂時計を下に配置。
  幅は `min(66vw,240px)`（時計）／`min(34vw,140px)`（砂時計）で可変。

## 時計
`clockFaceSVG()`（`assets/js/app.js`）で、以下の順にSVG要素を描画。

1. 外周のリング（`<circle fill="none">`、`stroke-width:2`）
2. 目盛り（12方向。3時間ごと=12/3/6/9の位置は長く・太い線）
3. 数字 12/3/6/9（`text-anchor="middle"` `dominant-baseline="central"`）
4. 長針（半径の0.72倍、`stroke-width:2.5`）
5. 短針（半径の0.48倍、`stroke-width:4`）※最後から2番目に描画
6. 中心のドット（`r=5`、針の根元を隠す。最後に描画）

**根本原因**: これまで `stroke="var(--chalk)"` `var(--chalk-dim)` `var(--chalk-faint)`
`fill="var(--accent-2)"` を指定していたが、これらのCSS変数は `style.css` の
`:root` に一切定義されていなかった（`--ink` `--ink-soft` `--accent` `--accent-warm`
のみが定義済み）。未定義の `var()` は初期値（strokeなら実質「描画されない」）に
解決されるため、リング・目盛り・数字・針・砂の一部が透明になっていた。

対策として、CSS変数への依存をやめ、`style.css` の `:root` と同じ実カラーコードを
JavaScript側の定数 `CLOCK_COLOR` に直接埋め込んだ。

- 短針: `#14181C`（`--ink` と同値）
- 長針: `#1F3A5F`（`--accent` と同値）
- リング・目盛り（主要）・数字・砂時計輪郭／キャップ: `#5A6169`（`--ink-soft` と同値）
- 目盛り（補助）: `#D8D4C8`（`--hairline` と同値）
- 中心ドット: 塗り `#1F3A5F`、縁取り `#FAFAF7`（`--paper` と同値）

描画順は上記の通り「リング→目盛り→数字→長針→短針→中心ドット」とし、
**針を他の要素より後（手前）に描くことで確実に視認できるようにした。**

## 砂時計
`hourglassSVG()`（`assets/js/app.js`）で、以下の順に描画。

1. **輪郭**（`<path fill="none" stroke="#5A6169">`）— 砂の量（ratio）に一切関係なく、
   常に描画する。上下2つの三角形が中央のくびれで接する形。
2. **上下の枠（キャップ）** — 上端・下端に太めの横線（`stroke-width:4`、
   `stroke-linecap="round"`）を、こちらも常に描画。
3. 上の砂（残り）— `fill="#9C4B32"` `opacity:.3`。`ratio>=0.999` のときのみ非表示。
4. 下の砂（経過）— `fill="#9C4B32"` `opacity:.85`。`ratio<=0.001` のときのみ非表示。
5. 落ちている砂の筋（任意、`0.001 < ratio < 0.999` のときだけ中央に細い縦線）。

砂の量の計算方法（`assets/js/app.js` 内、高さ比ベース）:

- 上の砂の表面位置 `t0 = ratio`（0のとき満杯＝三角形いっぱい、1のとき空）を用い、
  `topSurfaceY = gTop + t0 * (gMid - gTop)` で表面のY座標を求め、
  そこから首（`neckW`）までの台形を塗る。
- 下の砂の表面位置 `s0 = 1 - ratio`（0のとき空、1のとき満杯）を用い、
  `botSurfaceY = gMid + s0 * (gBot - gMid)` で表面のY座標を求め、
  そこから底（`halfW`）までの台形を塗る。
- 輪郭・キャップ・砂のいずれも同じ `ratio = clock.ratio`（0〜1にクランプ済み）を
  参照しているため、時刻表示・時計の針・砂の量は常に一致する
  （`verify_clock.js` で `#heroClock .sand-fall[data-ratio]` を用いて機械的に検証済み）。

砂時計の直下には `<p class="hero__hourglass-note">人生の <b>{ratio*100}%</b> が
過ぎました</p>` を常時表示し、経過割合を文字でも補足している。

## 目視確認の結果
Playwright で「数えはじめる」→生年月日入力→性別選択→「次へ」→
「全部数える」→「結果を見る」の実操作を行い、結果画面の `#hero` 部分を
390px（モバイル）・800px（デスクトップ）の両方でスクリーンショットし、
画像を読み込んで目視確認した。ローカル環境・本番環境
（`https://atonankai.pages.dev/`）の両方で確認済み。

| パターン | 生年月日 | 表示時刻 | 経過割合 |
|---|---|---|---|
| 若年 | 2005-06-15 | 06:16 午前 | 26.1% |
| 中年 | 1985-03-10 | 11:25 午前 | 47.6% |
| 高齢 | 1955-11-20 | 20:12 午後 | 84.2% |

チェック項目（8項目）は、3パターンすべて・390px/800pxの両方で以下の通り。

- [x] 時計の円（リング）が見えている
- [x] 短針と長針の2本が、はっきり見えている
- [x] 針の向きが、表示されている時刻と一致している
  （若年=06:16は短針が6と7の間、長針が3寄り／中年=11:25は短針が11過ぎ、
  長針が5寄り／高齢=20:12は短針が8過ぎ、長針が2寄り。いずれも一致）
- [x] 数字 12 / 3 / 6 / 9 が円周上の正しい位置にある
- [x] 砂時計が「砂時計の形」として認識できる（輪郭と上下の枠が見えている）
- [x] 砂の量が、経過割合と見た目で一致している
  （若年=上がほぼ満杯／中年=上下がほぼ半々／高齢=下がほぼ満杯）
- [x] 時計と砂時計が重なって読めなくなっていない（横並び・上下配置で分離済み）
- [x] 幅390px（モバイル）でも崩れず、はみ出していない
  （`document.documentElement.scrollWidth - clientWidth = 0` を実測で確認）

## アセットバージョン
`?v=20260815d`（`index.html` / `about.html` / `privacy.html` / `terms.html` /
`disclaimer.html` / `contact.html` の全HTMLファイルで統一して更新済み）。

## 確認できなかった項目
なし。
