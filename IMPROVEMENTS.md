# 改修指示書 — v2

このファイルの内容をすべて実装してください。
既存の計算ロジック（`assets/js/calc.js` / `assets/js/data.js`）の**数式と統計値は絶対に変更しないこと**。
厚生労働省の公表値に合わせてフィッティング済みの生命表であり、作り直すと精度が失われます。
変えるのは見た目・操作性・情報設計です。

作業の最後に、`node test.js`（jsdomが必要なら `npm install jsdom`）を実行して全項目が通ることを確認してください。
テストが落ちた場合は、テスト側ではなく実装側を直してください。ただしDOM構造の変更に伴う正当な修正はテスト側でも構いません。

---

## 0. 最優先で直すバグ

### 0-1. 人生時計の針が時刻と一致していない【重大】

現状 `assets/js/app.js` の `clockSVG()` は、24等分の目盛りの上に「進捗率 × 360度」で針を描いている。
そのため「10:13」と表示されているのに針は約153度（2時半あたり）を指しており、完全に矛盾している。

**正しい実装に置き換えること。**

- 文字盤は **12時間のアナログ時計**とする（12・3・6・9の数字を表示、他は目盛り）
- **短針** = `((hour % 12) + minute / 60) / 12 × 360度`
- **長針** = `minute / 60 × 360度`
- 午前・午後が分かるよう、時刻表示の脇に「午前」「午後」を出す（`hour < 12` で午前）
- 針は必ず表示時刻と一致させること。実装後、いくつかの生年月日で目視確認すること

### 0-2. 公開ドメインの表記が古い

`index.html` / `robots.txt` / `sitemap.xml` の中の `atonankai.com` を、すべて実際の公開URL
`atonankai.pages.dev` に置換すること（`og:url`、`canonical`、`og:image`、`Sitemap:`、`<loc>`）。

### 0-3. 生年月日フォームが画面からはみ出す

iPhoneで `<input type="date">` が枠を超えている。下記「3. 生年月日の入力方式」で作り直すため、
`type="date"` 自体を廃止する。

---

## 1. デザインの全面刷新

### 1-1. 現状の問題

蛍光オレンジ・蛍光ピンクの多用、丸みの強いポップ書体、太い影付きボタン。
これらが「素人が作った」「AIが自動生成した」印象を生んでいる。
統計と積分に基づく精密な計算をしているサイトとして、まったく釣り合っていない。

### 1-2. 目指す方向

**大学の研究室の黒板。** 映画で科学者が数式を書き連ねている、あの画。
知的で、静かで、少し緊張感がある。けれど冷たすぎず、読みやすい。

### 1-3. カラートークン（`assets/css/style.css` の `:root` を全面差し替え）

```
--slate:       #17201D   /* 黒板の地。緑がかった暗色。真っ黒にしない */
--slate-deep:  #101715   /* さらに沈めたい面 */
--slate-soft:  #1F2A26   /* カードやパネルの面 */
--chalk:       #EDEAE0   /* チョークの白。純白にしない */
--chalk-dim:   #9AA5A0   /* 補助テキスト */
--chalk-faint: #4A5751   /* 罫線・目盛り */
--accent:      #D9C86A   /* 淡い黄チョーク。主要な数字に */
--accent-2:    #C98C7A   /* 淡い赤チョーク。強調・警告に */
--accent-3:    #8FB4B0   /* 淡い青チョーク。補足に */
```

**蛍光色は全廃。** 彩度を落としたチョーク調で統一する。
これらを差し替えるだけで白基調に転換できるよう、色は必ず変数経由で参照すること。

### 1-4. タイポグラフィ

現在の `Dela Gothic One`（丸くて太いポップ書体）が子供っぽさの主因。**廃止する。**

Google Fonts から以下を読み込む。

- **表題・見出し**: `Shippori Mincho B1`（600 / 800）— 明朝体。知性と静けさ
- **本文**: `Zen Kaku Gothic New`（400 / 500 / 700）— 現状のまま流用可
- **数値**: `Outfit`（200 / 300 / 500）— 細身の幾何学サンセリフ。大きな数字を上品に見せる
- **キャプション・出典**: `Roboto Mono`（400）— 現状のまま
- **背景の数式**: `Caveat`（400 / 600）— 手書き風。チョークの筆致に近い

大きな数字は **細いウェイト（200〜300）で大きく** 組むこと。太字で大きくすると安っぽくなる。

### 1-5. ボタン

現状の `box-shadow: 4px 4px 0` という「ずれた影」は、ポップだが安く見える。**廃止。**

- 塗りボタン: `--accent` の細い枠線 + 半透明の塗り、ホバーで塗りが濃くなる
- 角丸は `4px` 程度まで抑える（現在14pxは丸すぎる）
- 押下時は影ではなく、わずかな明度変化と `transform: translateY(1px)` で表現

---

## 2. トップページ（表紙）の作り直し

### 2-1. 背景に数式を敷く【このサイトの顔になる部分】

黒板にチョークで数式が書き連ねられている背景を作る。**実装方法は問わないが、以下を守ること。**

- **本文の可読性を絶対に損なわないこと。** 数式の不透明度は `0.06〜0.10` 程度。目を凝らせば読める、くらいが正解
- 数式は `Caveat` で描画し、**要素ごとに `-4deg 〜 +4deg` のランダムな傾き**をつける（手書き感）
- 不透明度も要素ごとに少しばらつかせる
- 画面全体に散らすが、中央の題字周辺は密度を下げて可読性を確保する
- スクロールに対して**ごくゆっくり視差（パララックス）**で動かすと奥行きが出る。ただし `prefers-reduced-motion` では停止すること
- モバイルでは数式の数を減らして負荷を抑える（DOM要素数に注意）

**実際にこのサイトが使っている式を必ず含めること**（下記は必須。これがサイトの主張そのもの）。

```
e(x) = ∫₀^∞ l(x+t) / l(x) dt
l(x) = exp( −∫₀ˣ μ(s) ds )
μ(x) = A + B·e^(Cx)
q(x) = 1 − e^(−μ(x))
N = f · ∫₀^∞ S₁(t)·S₂(t) dt
P(both alive at t) = S₁(t) × S₂(t)
S(t) = l(x+t) / l(x)
```

**雰囲気を作るための数式も混ぜてよい**（密度が欲しいため）。ただし出鱈目な記号の羅列は避け、
統計学・確率論・微積分の実在する表現を使うこと。例:

```
Σ (n=1→∞)     lim (t→∞)     ∂P/∂t     E[X] = ∫ x·f(x) dx
Var(X) = E[X²] − E[X]²      f(x) = dF(x)/dx      Γ(n) = (n−1)!
81.09    87.13    72.57    75.45    l(65) = 0.896    l(90) = 0.258
```

数字（平均寿命や生存割合の実測値）を混ぜると、このサイト固有の背景になる。

### 2-2. 題字

```
あと何回。
```

- `Shippori Mincho B1` 800、大きく、字間をわずかに詰める
- 現在の「版ズレ（オレンジとピンクの二重影）」は**廃止**。あれがポップさの原因
- 代わりに、チョークで書いた線が紙に残るような**極薄い白のにじみ**を1層だけ添える程度に留める
- 題字の下に、チョークで引いたような**わずかに歪んだ横線**を1本引く（定規を使わず引いた線の質感）

### 2-3. 副題「人生の残量計算機」は削除

不自然なので消す。代わりに、下記のリード文を置く。

### 2-4. リード文（明朝体・大きめ・行間広め）

```
あなたに残された週末は、あと何回だろう。
家族と同じ食卓を囲めるのは、あと何回だろう。
```

### 2-5. 説明文（このサイトが「適当な計算ではない」ことを伝える）

リード文の下に、以下の趣旨を**読みやすい平易な日本語**で置く。
硬い専門用語を並べるのではなく、「ちゃんと作られている」と自然に伝わる文章にすること。
下記は内容の指定であり、一字一句この通りである必要はない。より良い日本語があればそちらを採用すること。

要点:

- 平均寿命から今の年齢を引く、という計算はしていない
- 厚生労働省が公表している生命表をもとに、0歳から110歳までの1歳きざみの生存率曲線を作ってある
- 誰かに会える回数は、「自分と相手が同時に生きている確率」を時間で積み上げて出している
- そこには、相手が先に亡くなる可能性も、自分が先に亡くなる可能性も含まれている
- だから「平均で何年」では出てこない数字が出る

### 2-6. 安心感の明示【重要・目立たせる】

現在は小さなタグで控えめに出ているだけ。**もっとはっきり、目に入る位置に置く。**

```
メールアドレスも、名前も、会員登録も要りません。
入力した内容はあなたの端末の中だけで計算され、どこにも送信されません。
```

チョークで囲った枠のような装飾を添えて、独立したブロックとして見せる。

### 2-7. 「数えはじめる」ボタン

現在の蛍光オレンジの塗りボタンから、チョークの手描き枠のような静かなボタンへ。
枠線は `--accent`、内側は半透明。ホバー・タップで内側が満ちる。

---

## 3. 生年月日の入力方式を変える

### 3-1. 現状の問題

`<input type="date">` は、iPhoneで開くと**今日の日付**が起点になる。
1990年生まれの人は、そこから30年以上スクロールさせられる。面倒で離脱の原因になる。
さらに枠からはみ出して表示が崩れている。

### 3-2. 変更内容

`type="date"` を廃止し、**「年・月・日」の3つのプルダウン**に分ける。

- **年**: 現在の年 − 100 から 現在の年 まで。**降順**（新しい年が上）で並べる。初期選択は空
- **月**: 1〜12
- **日**: 1〜31。選択された年月に応じて日数を自動調整すること（2月30日などを選べないように）
- 3つとも選択された時点で年齢を計算し、既存の `#ageHint` に反映する
- プルダウンは黒板デザインに合わせ、`--slate-soft` の背景にチョーク色の文字、細い枠線
- **`<select>` のネイティブ表示に頼らず、必ずスタイルを当てること**（`appearance: none` + 独自の矢印）
- 横幅は必ず親要素に収める。`box-sizing` と `min-width: 0` に注意

### 3-3. 内部データの互換性

`S.birth` には従来どおり `Date` オブジェクトを入れること。`exactAge()` の引数仕様は変えない。

---

## 4. 結果画面

### 4-1. 人生時計＋砂時計【最重要の見せ場】

上記 0-1 のバグを直したうえで、**時計の中に砂時計を入れる**。

構成:

- **外側**: 12時間のアナログ時計の文字盤（12・3・6・9の数字、細い目盛り、短針・長針）
- **内側（中央）**: 砂時計のシルエット。上側に残り、下側に落ちた分が溜まっている
  - 砂の落ち具合が、人生の経過割合（`ratio`）と一致すること
  - 上の砂 = 残り、下の砂 = 過ぎた分
  - 落ちている砂の細い筋を1本描くと生きた絵になる
- 全体をチョークで描いた線画のように見せる（塗りは最小限、線が主役）
- 時刻の数字（例: `10:13`）は時計の下に `Outfit` の細ウェイトで大きく置き、脇に「午前 / 午後」

**砂時計の減り具合と、時計の針の位置と、表示している時刻。この3つが必ず一致していること。**
実装後、複数の年齢で目視確認すること。

### 4-2. 残された週末の表現を変える

現在は2,000個以上のドットを並べている。視覚的には強いが、砂時計と役割が重複し、
DOM要素も多すぎてスマートフォンで重い。**ドットは廃止する。**

代わりに:

- 大きな数字（`Outfit` 細ウェイト）で回数を出す
- その下に**細い横罫を1本**引き、過ぎた分と残りを色で塗り分ける（高さ4px程度の控えめなもの）
- 罫の両端に小さく「生まれた日」「◯歳」とキャプションを添える
- 補足として「これは、あと◯回の土曜と日曜のこと」のような一文を置く

コンパクトで、砂時計と喧嘩しないこと。

### 4-3. 結果カードの意匠

現在の「塗りつぶし＋ずれた影」を廃止し、以下に変更。

- 背景は `--slate-soft`、枠線は `--chalk-faint` の細線（1px）
- 影は使わない。使うとしても極めて弱い拡散影のみ
- 数字は `Outfit` 200〜300 で大きく
- ラベルは `Roboto Mono` の小さな字で、字間を広めに
- 出典表記は現状どおり各カードに残すこと（信頼性の根拠なので削らない）

---

## 5. 18歳以上向けセクションの出し方を変える

### 5-1. 現状の問題

全員の結果画面の下に無条件で表示されている。求めていない人にとっては不快。

### 5-2. 変更内容

- `assets/js/app.js` の `CATS` 配列に、新しい項目を追加する

```js
{ id: 'partner', t: 'パートナーとの時間', d: '18歳以上の方のみ・同意の確認があります', adult: true }
```

- この項目には、一覧上で **「18+」** の小さな印を付ける（`adult: true` を見て描画）
- **STEP2でこの項目を選んだ人にだけ**、結果画面に18歳確認のゲートを表示する
- 選んでいない人には、ゲートもセクションも**一切表示しない**（DOMごと出さない）
- 「全部数える」ボタンを押した場合も、この項目だけは**自動で選ばれないようにする**
  （`adult: true` の項目は `pickAll` の対象外にする）
- 18歳確認のゲート自体は現状の作りを維持する。ただし配色は新しいチョーク基調に合わせる

---

## 6. フッターと法務ページの整備

現在のフッターは出典と計算方法だけで、個人が趣味で作ったサイトに見える。
きちんと運営されているサイトとして成立する水準に引き上げる。

### 6-1. 新規に作成するページ

以下を**独立したHTMLファイル**として作成する。トップと同じデザイン（黒板・チョーク）で統一し、
共通のヘッダー（サイト名クリックでトップへ戻る）とフッターを持たせること。

| ファイル | 内容 |
|---|---|
| `about.html` | このサイトについて。目的、計算の考え方、運営者情報 |
| `privacy.html` | プライバシーポリシー |
| `terms.html` | 利用規約 |
| `disclaimer.html` | 免責事項 |
| `contact.html` | お問い合わせ |

### 6-2. 各ページの中身

**すべて自然な日本語で書くこと。** 翻訳調・テンプレート丸写しの文章にしないこと。
法律文書だからといって不必要に難解にせず、読んで意味が分かる日本語にすること。

#### `privacy.html` に必ず含める内容

- このサイトは、利用者が入力した生年月日・性別・生活習慣などの情報を、**一切サーバーへ送信していない**こと
- すべての計算は利用者のブラウザの中で完結し、入力内容はページを閉じた時点で消えること
- cookie を使用していないこと
- ただし、以下については外部への通信が発生することを正直に書くこと
  - Webフォント（Google Fonts）の読み込み
  - SNSの共有ボタンを押した場合の各SNSへの遷移
  - ホスティング事業者（Cloudflare）によるアクセスログの記録
- アクセス解析ツールを導入した場合は、この項目を更新する旨を明記
- 改定時の告知方法
- 制定日・最終改定日の欄（実装時点の日付を入れる）

#### `terms.html` に必ず含める内容

- 本サービスの利用条件
- 計算結果は統計上の期待値であり、特定の個人の寿命を予測するものではないこと
- 禁止事項（不正アクセス、自動化された大量アクセス、サービスの複製・再配布など）
- 知的財産権の帰属。ただし引用している統計データの権利は各公表機関に帰属すること
- サービス内容の変更・中断・終了の可能性
- 準拠法は日本法、管轄は日本の裁判所

#### `disclaimer.html` に必ず含める内容

**ここは特に正直に、かつ誠実に書くこと。** このサイトの信頼性はここで決まる。

- 計算結果は、厚生労働省の生命表など公的統計に基づく**統計上の期待値**であること
- 個人の寿命・健康状態・人間関係を予測するものではないこと
- 医学的な診断や助言ではないこと。健康上の不安がある場合は医療機関に相談すること
- 統計は集団の傾向を示すものであり、個人がその通りになるわけではないこと
- 年齢による頻度の変化を含む一部の項目は、調査で観察される傾向に形を合わせた**モデル**であり、
  特定の調査の実測値そのものではないこと
- 元の統計が更新された場合、結果が変わりうること
- 計算方法や実装に誤りが含まれる可能性を否定できないこと。**誤りを見つけた場合は問い合わせ窓口へ**
- 本サイトの利用によって生じたいかなる損害についても責任を負いかねること
- 数字を見て強い不安を感じた場合に備え、**心の健康について相談できる公的な窓口があること**に、
  押し付けがましくない書き方で触れること（具体的な窓口名は書かず、「お住まいの自治体の相談窓口」
  「かかりつけ医」といった案内に留めること）

#### `contact.html` に必ず含める内容

- 問い合わせの受付方法（**現時点では準備中である旨を、正直に、かつ誠実に書く**）
- 受け付ける内容の例（計算方法の誤りの指摘、統計データの更新情報、掲載内容に関するご指摘など）
- 返信を確約できない旨
- 将来的にフォームまたはメールアドレスを設置する予定である旨
- **架空の連絡先やダミーのメールアドレスは絶対に書かないこと。** 嘘は信頼を最も速く壊す

#### `about.html` に必ず含める内容

- なぜこのサイトを作ったのか（終わりを意識すると、今が濃くなる、という趣旨）
- 計算の考え方（トップページより詳しく。数式を出してよい）
- 使用している統計の一覧と出典リンク
- 生命表の再現精度（平均余命の誤差 最大0.065年、生存割合の誤差 最大0.24ポイント）
- 運営者情報

### 6-3. 運営者情報の扱い【重要】

運営者名は**まだ決まっていない**。以下のように実装すること。

- 表記は `あと何回。運営` とする（仮の名称）
- 該当箇所のHTMLに、以下のコメントを必ず入れること

```html
<!-- TODO: 運営者名。正式名称が決まったら、全ページのこの箇所を差し替えること -->
```

- 所在地・電話番号などは**書かない**（個人運営で書けないものを書くと、かえって不自然かつ危険）
- 「個人が運営しています」という事実を、隠さず、しかし卑下せずに書くこと
- 差し替えるべき箇所を一覧にした `TODO.md` をリポジトリ直下に作成し、
  「正式名称が決まったら書き換える場所」「問い合わせ先を設置する場所」をまとめておくこと

### 6-4. フッターの構成

全ページ共通で、以下を含むフッターにする。

- サイト名と、ひとことの説明
- ナビゲーション: このサイトについて / プライバシーポリシー / 利用規約 / 免責事項 / お問い合わせ
- 使用している統計の出典一覧（現状のものを維持）
- 計算方法の要約（現状のものを維持）
- 「本サイトの計算結果は統計上の期待値であり、個人の寿命を予測するものではありません」という
  1行の注意書きを、常に見える位置に
- 著作権表記（実装年を入れる）

**大企業のサイトのように見せる**とは、装飾を増やすことではない。
情報が整理され、書くべきことが漏れなく書かれ、余白と罫線が規律正しく効いている状態を指す。
リンクは詰め込まず、階層と間隔で整理すること。

---

## 7. 全体の操作感

「使い続けたくなる滑らかさ」を作る。ただし**動きを足しすぎると逆に安っぽくなる**ため、抑制的に。

- 画面遷移（STEP1→2→3→結果）に、`180〜240ms` のフェード＋わずかな上方向スライドを入れる
- 結果カードは、上から順に `40ms` ずつ遅らせて現れる（一斉に出さない）
- 大きな数字は、`0` から実際の値まで **600ms 程度でカウントアップ**させる（`ease-out`）
- 砂時計の砂は、表示時に上から下へ落ちるアニメーションを1回だけ再生する
- ボタンのホバー・タップは `120ms` 程度の短い変化に留める
- **`prefers-reduced-motion: reduce` の場合は、すべてのアニメーションを無効化すること**（既存の記述を維持・拡張）
- スクロールは滑らかに（`scroll-behavior: smooth`）。ただし上記の reduced-motion では無効化

---

## 8. 実装上の注意

- **既存の計算ロジックには手を触れないこと。** `calc.js` の関数仕様と `data.js` の数値は不変
- `[hidden] { display: none !important }` の記述を消さないこと（表紙が消えなくなるバグの対策）
- 画面幅 320px（iPhone SE相当）で、**横スクロールが発生しないこと**を必ず確認する
- タップ対象は最低 44×44px を確保する
- キーボード操作時のフォーカス表示（`:focus-visible`）を必ず残すこと
- 背景の数式は `aria-hidden="true"` を付け、読み上げソフトが読まないようにすること
- 新規ページにも OGP メタタグを付けること
- `sitemap.xml` に新規ページのURLを追加すること
- 作業完了後、`node test.js` を実行し、全項目が通ることを確認すること
- すべて完了したら commit して push すること。Cloudflare Pages が自動で再デプロイする

---

## 9. 完了後に報告してほしいこと

1. 人生時計の針が、表示時刻と一致していることを確認したか（確認した年齢の例を挙げて）
2. 砂時計の減り具合が、経過割合と一致していることを確認したか
3. 320px幅で横スクロールが起きないことを確認したか
4. `node test.js` が全項目通過したか
5. 新規に作成したファイルの一覧
6. `TODO.md` に記載した「後で差し替えるべき箇所」の一覧
